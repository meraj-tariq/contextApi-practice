import en from './en';
import de from './de';
import tr from './tr';

export const dictionaryList = { en, de, tr }

export const languageOptions = {
    en: 'English',
    tr: 'Türkçe',
    de: 'Deutsch'
}